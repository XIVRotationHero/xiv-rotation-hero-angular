import { ActionByIdPipe } from './action-by-id.pipe';

describe('ActionByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
